// this should be the entry point to your library
import RenderCounter, { withRenderCount } from './RenderCounter';
import Image, { withImage } from './Image';

module.exports = {
  Image,
  withImage,
  RenderCounter,
  withRenderCount,
};
